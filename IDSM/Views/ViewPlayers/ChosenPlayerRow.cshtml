@model IDSM.Model.Player
@using IDSM.Helpers

<div class="playerRow">

    @using(Html.BeginCollectionItem("players")) {
        <p>@Html.DisplayFor(m=>m.Name)<a href="#" class="deleteRow">delete</a></p>
        
        @Html.HiddenFor(m=>m.Name)
        @Html.HiddenFor(m=>m.Nation)
        @Html.HiddenFor(m=>m.Club)
        @Html.HiddenFor(m=>m.Position)

    }
</div>  